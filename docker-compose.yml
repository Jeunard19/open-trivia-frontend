version: '3.8'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:80'
    restart: unless-stopped
    # To connect to a backend running on the host machine,
    # use host.docker.internal on Docker Desktop or configure a separate service.
